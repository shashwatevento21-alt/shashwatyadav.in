name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    name: FTP Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Deploys only the static site. /public_html/creators/ (the Laravel
      # Creator Marketplace app) is managed entirely outside this pipeline
      # and must never be touched here — excluded below, and no step in
      # this workflow ever targets server-dir /public_html/creators/.
      - name: Deploy main site to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          # The FTP account's own root is already public_html/ (confirmed via
          # hPanel: Directory = /home/u322722500/domains/shashwatyadav.in/public_html)
          # — do NOT set this to /public_html/, that creates a duplicate nested
          # public_html/public_html/ folder and can corrupt the delete-sync logic.
          server-dir: /
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/CNAME
            **/deploy-creators-subfolder
            **/deploy-creators-subfolder/**
            creators
            creators/**
