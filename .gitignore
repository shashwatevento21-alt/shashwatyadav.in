# /creators is the Laravel Creator Marketplace app, deployed and managed
# entirely outside this repo. It must never be tracked, uploaded, or
# considered "extra"/removable by any git or deploy tooling that operates
# on this working tree.
creators/

# Chatbot OpenRouter API key — server-side secret, never committed.
# Must be uploaded to Hostinger manually (outside the git deploy pipeline),
# see chatbot/DEPLOY.md.
chatbot/chat-config.php

# Local dev-server config (Claude Code preview only) — not needed on Hostinger.
.claude/
